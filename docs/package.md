| 场景 | 命令 |
|------|------|
| 首次部署 | `npm run init-app` |
| 初始化标签 | `npm run init-tags` |
| 创建默认收藏夹 | `npm run create-default-folders` |
| 同步统计数据 | `npm run sync-counts` |
| 修复权限 | `npm run fix-permissions` |
| 修改用户角色 | `npm run manage-roles <action> <email>` |

所有脚本职责清晰，没有重复！🎉